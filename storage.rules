rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Logos de empresas: lectura pública, escritura solo usuarios autenticados
    match /logos/{empresaId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
